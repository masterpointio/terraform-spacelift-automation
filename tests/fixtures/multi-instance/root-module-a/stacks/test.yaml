kind: StackConfigV1
stack_settings:
  space_id: direct-space-id-stack-yaml # Tests direct space_id precedence over global variable space_id
  worker_pool_name: example-worker-pool # Tests worker_pool_name gets translated to worker_pool_id
  aws_integration_name: example-aws-integration # Tests aws_integration_name gets translated to aws_integration_id
  labels:
    - test_label
